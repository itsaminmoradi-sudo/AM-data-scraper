// background.js

// This service worker is currently a placeholder.
// It can be used for future features like handling multiple URLs
// or managing long-running tasks.

chrome.runtime.onInstalled.addListener(() => {
    console.log('AM Data Scraper extension installed.');
});
